# 依赖与构建产物 —— 镜像里由 npm ci / vite build 重新生成
node_modules
**/node_modules
**/dist

# 版本库与本地工具配置
.git
.gitignore
.claude
.clone
.DS_Store

# 文档不进镜像
**/*.md
docs

# 注意:不要按游戏名排除源码。构建上下文是 repo 根,四个游戏共用它和这一份
# .dockerignore;按游戏名排除会让另外三个的构建拿不到自己的源码。
# (镜像里实际 COPY 哪些路径由 platform/deploy/Dockerfile 的 ARG GAME_DIR 决定,
#  每个镜像只带自己那一个游戏。)
