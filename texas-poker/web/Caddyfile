# Internal front door for Texas Poker. TLS is handled by the shared proxy.

:80 {
	encode gzip

	handle /socket.io/* {
		reverse_proxy texas-poker-server:3001
	}

	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
