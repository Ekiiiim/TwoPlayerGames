{
  "name": "twoplayergames-tooling",
  "version": "0.0.0",
  "private": true,
  "description": "Repo-wide dev tooling (Prettier formatting). Each game stays a self-contained npm monorepo; this root only holds shared formatting config.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-svelte": "^3.2.6",
    "svelte": "^4.2.0"
  }
}
